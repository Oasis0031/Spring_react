스타일 앱

설치 명령어 터미널에서
yarn add styled-reset styled-components @fortawesome/free-solid-svg-icons @fortawesome/react-fontawesome @fortawesome/fontawesome-svg-core
								폰트를 아이콘처럼 쓸 수 있게하는 라이브러리
리액트-스타일드 컴포넌트를 사용함. 테일윈드css도 쓰긴 하는데 스타일드 쪽을 더 잘 씀


커스텀 훅스 패턴

[components]
[utils]
   - arrayheler.js
   - parseheler.js
[consts]
   - key.js (상수, 문자열)
[styles]
   - theme.js
   - globalStyle.js
   - common.js
   ...
[pages]
   [Home]
      [Hooks]
      [Components]
      -home.js
[routes]
[hooks]
... etc

관심사의 분리
재사용성, 유지보스

깔면 app.js 내용 초기화하고, index.js도 app만 남김

yarn add react-router-dom
사용을 위해 설치
라우터를 작성했으면, 라우터 프로바이더를 app.js에 적어두자

global.js
- reset, font-family, color

createGlobalStyle을 사용한다.

common.js
- 공통 스타일
   flex 등

theme.js
- 스타일 정의 가이드 컬러들

js에서 css를 못 쓰기 때문에 그걸 도와주는 것이 스타일드컴포넌트
기본 문법
styled.p``
스타일드.요소.``

확장요소 vscode-styled-components 다운


import React from 'react';
import styled from 'styled-components';

const StyledP = styled.p`
  color: blue;
  font-size: 20px;
  font-weight: 800;
` 

const Styled01 = () => {


  return (
    <div>
      <StyledP>스타일 넣기</StyledP>
    </div>
  );
};

export default Styled01;

이렇게 쓰지 말고, StyledP는 외부로 빼자.
한 파일에는 하나의 언어만 쓴다.

import styled from "styled-components";

export const StyledP = styled.p`
  color: blue;
  font-size: 20px;
  font-weight: 800;
` 

